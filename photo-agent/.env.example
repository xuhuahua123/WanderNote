# WanderNote Photo Agent 环境变量配置
#
# 使用方法：
# 1. 复制此文件为 .env
# 2. 填写真实的 agent_token
# 3. .env 文件不会被提交到代码仓库

# Agent 认证 token（优先于 config.yaml 中的配置）
AGENT_TOKEN=

# 照片根目录（可选，优先于 config.yaml 中的配置）
PHOTO_ROOT=

# 后端服务器地址（可选，优先于 config.yaml 中的配置）
SERVER_URL=
